
Files for 604 and All are identical; normally this would be taken care of by using links, 
but LaTeX doesn't like links, so the MathFactsIndexAll directory is copied by the make604 directory.  It should not be modified.

